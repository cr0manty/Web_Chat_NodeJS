<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/">Chat App</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <a class="nav-item nav-link" href="/room/">Room</a>
        </div>
        <% if(is_auth) { %>
            <div class="navbar-nav">
                <a class="nav-item nav-link<% if(!is_auth) { %> disabled<%}%>" href="/create">Create room</a>
            </div>
        <% } %>
    </div>
    <div class="navbar-nav">
        <% if(is_auth) { %>
            <div class="navbar-nav">
                <a class="nav-item nav-link" href="/profile/<%= user.username %>"><%= user.username %></a>
            </div>
            <div class="navbar-nav">
                <a class="nav-item nav-link" href="/logout">Logout</a>
            </div>
        <% } else { %>
            <div class="navbar-nav">
                <a class="nav-item nav-link" href="/signup">Sign up</a>
            </div>
            <div class="navbar-nav">
                <a class="nav-item nav-link" href="/signin">Sign in</a>
            </div>
        <% } %>
    </div>

</nav>
